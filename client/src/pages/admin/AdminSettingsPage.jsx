import { FiCheckCircle, FiCloud, FiLock, FiServer } from 'react-icons/fi'
import AdminPageHeader from '../../components/admin/AdminPageHeader.jsx'
function AdminSettingsPage() { const settings = [{ icon: FiServer, title: 'Application API', text: import.meta.env.VITE_API_URL || 'http://localhost:5000/api', status: 'Configured' }, { icon: FiCloud, title: 'Cloudinary media', text: 'Credentials are stored only in the server environment.', status: 'Server managed' }, { icon: FiLock, title: 'Admin security', text: 'HTTP-only JWT cookie with server-side role authorization.', status: 'Protected' }]; return <div className="space-y-6"><AdminPageHeader title="Settings" description="Operational configuration for the Eshaz Dream World administration environment." /><div className="grid gap-5 lg:grid-cols-3">{settings.map(({ icon: Icon, title, text, status }) => <article key={title} className="form-section"><span className="grid h-11 w-11 place-items-center rounded-xl bg-pink-light text-rosewood"><Icon /></span><h2 className="mt-5 font-serif text-2xl font-semibold">{title}</h2><p className="mt-2 min-h-12 text-sm leading-6 text-muted">{text}</p><p className="mt-5 flex items-center gap-2 text-xs font-semibold text-green-700"><FiCheckCircle /> {status}</p></article>)}</div><section className="form-section"><h2 className="font-serif text-2xl font-semibold">Environment-managed settings</h2><p className="mt-3 max-w-3xl text-sm leading-6 text-muted">Secrets, database connections, Cloudinary credentials, cookie policy, and allowed frontend origins are intentionally managed through server environment variables. They are never exposed or editable in the browser.</p></section></div> }
export default AdminSettingsPage
