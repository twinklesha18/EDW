import { FiCheckCircle, FiEdit2, FiMapPin, FiTrash2 } from 'react-icons/fi'
function AddressCard({ address, onEdit, onDelete, onDefault, loading }) { return <article className={`relative rounded-[1.5rem] border bg-white p-5 ${address.isDefault ? 'border-gold/60 shadow-[0_12px_35px_-28px_rgba(59,47,54,.5)]' : 'border-gold/15'}`}><div className="flex items-start justify-between gap-4"><div className="flex gap-3"><span className="mt-1 grid h-9 w-9 shrink-0 place-items-center rounded-full bg-pink-light text-rosewood"><FiMapPin /></span><div><div className="flex flex-wrap items-center gap-2"><h2 className="font-semibold">{address.label}</h2>{address.isDefault && <span className="rounded-full bg-[#edf8f1] px-2.5 py-1 text-[.65rem] font-bold uppercase tracking-wide text-[#39704f]">Default</span>}</div><p className="mt-3 text-sm font-medium">{address.fullName}</p><p className="mt-1 text-sm leading-6 text-muted">{address.addressLine1}{address.addressLine2 && `, ${address.addressLine2}`}<br />{address.city}, {address.district}, {address.province}{address.postalCode && ` ${address.postalCode}`}<br />{address.country}<br />{address.phone}</p></div></div></div><div className="mt-5 flex flex-wrap gap-2">{!address.isDefault && <button type="button" className="secondary-button min-h-9 px-3 text-xs" onClick={() => onDefault(address._id || address.id)} disabled={loading}><FiCheckCircle /> Set Default</button>}<button type="button" className="secondary-button min-h-9 px-3 text-xs" onClick={() => onEdit(address)}><FiEdit2 /> Edit</button><button type="button" className="secondary-button min-h-9 px-3 text-xs text-red-600" onClick={() => onDelete(address)}><FiTrash2 /> Delete</button></div></article> }
export default AddressCard
