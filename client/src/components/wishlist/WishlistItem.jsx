import { FiEye, FiShoppingBag, FiTrash2 } from 'react-icons/fi'
import { Link } from 'react-router-dom'
import { formatCurrency } from '../../utils/formatCurrency.js'
function WishlistItem({ item, onRemove, onMove, disabled }) { return <article className="overflow-hidden rounded-[1.5rem] border border-gold/15 bg-white"><Link to={`/product/${item.slug}`}><img src={item.image} alt={item.name} className="aspect-[4/3] w-full object-cover" /></Link><div className="p-5"><p className="text-[.68rem] font-semibold uppercase tracking-wider text-gold">{item.category}</p><Link to={`/product/${item.slug}`} className="mt-2 block font-serif text-xl font-semibold hover:text-rosewood">{item.name}</Link><p className="mt-3 font-semibold text-rosewood">From {formatCurrency(item.price)}</p><button type="button" onClick={() => onMove(item)} disabled={disabled} className="primary-button mt-5 w-full"><FiShoppingBag /> Add Size S</button><div className="mt-2 grid grid-cols-2 gap-2"><Link to={`/product/${item.slug}`} className="secondary-button min-h-10 px-3 text-xs"><FiEye /> Choose Size</Link><button type="button" onClick={() => onRemove(item.productId)} disabled={disabled} className="secondary-button min-h-10 px-3 text-xs text-red-600"><FiTrash2 /> Remove</button></div></div></article> }
export default WishlistItem
