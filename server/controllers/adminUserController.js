import User from '../models/User.js'
import { escapeRegex, paginationData, paginationFromQuery } from '../utils/queryUtils.js'
import { AppError, sendSuccess } from '../utils/responseUtils.js'

export async function listUsers(request, response) { const { page, limit, skip } = paginationFromQuery(request.query, { defaultLimit: 10 }); const filter = {}; if (request.query.search) { const pattern = new RegExp(escapeRegex(request.query.search), 'i'); filter.$or = [{ firstName: pattern }, { lastName: pattern }, { email: pattern }, { phone: pattern }] } if (request.query.role) filter.role = request.query.role; if (request.query.status === 'active') filter.isActive = true; if (request.query.status === 'inactive') filter.isActive = false; const [users, total] = await Promise.all([User.find(filter).sort({ createdAt: -1 }).skip(skip).limit(limit), User.countDocuments(filter)]); return sendSuccess(response, { message: 'Users retrieved', data: { users, pagination: paginationData(total, page, limit) } }) }
export async function updateUserAccess(request, response) { const user = await User.findById(request.params.id); if (!user) throw new AppError('User not found', 404); if (String(user._id) === String(request.user._id) && (request.validatedBody.role === 'user' || request.validatedBody.isActive === false)) throw new AppError('You cannot remove your own admin access', 409); Object.assign(user, request.validatedBody); await user.save(); return sendSuccess(response, { message: 'User access updated successfully', data: { user } }) }
